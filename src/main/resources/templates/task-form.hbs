{{#partial "content"}}
   {{#if editTask}}
       <h1>Edit this Section!</h1>
       <form action="/tasks/{{task.id}}" method="post">
          <br>
          <label for="description">Edit this Section's description</label>
          <input id="description" name="description" type="text" value="{{task.description}}">
          <br>
          <label for="categoryId">Update this Section's Department:</label>
          <select id="categoryId" name="categoryId">
             {{#each categories}}
                <option value="{{id}}">{{name}}</option>
             {{/each}}
          </select>
          <p>Don't see the right Department?<a href="/categories/new">Add a new one first.</a></p>
          <button type="submit" class="btn btn-default">Add!</button>
       </form>
   {{else}}
       <h2>Add a new Section!</h2>
       <form action="/tasks" method="post">
         {{#if categories}}
             <label for="description">Section Description</label>
             <input id="description" name="description" type="text">
             <label for="categoryId">Categorize this Section as:</label>
             <select id="categoryId" name="categoryId">
                 {{#each categories}}
                     <option value="{{id}}">{{name}}</option>
                 {{/each}}
             </select>
             <p>Don't see the right Department?<a href="/categories/new">Add a new one first.</a></p>
         {{else}}
             <h3> You'll need to add a Department before you add a Section. <a href="/categories/new">Add one here.</a> </h3>
         {{/if}}
         <button type="submit" class="btn btn-default">Add!</button>
       </form>
   {{/if}}
{{/partial}}

{{> layout.hbs}}
